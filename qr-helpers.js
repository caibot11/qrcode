// Re-export shim for backward compatibility
export {
  easeInOutCubic, easeOutCubic, lerp, hexToRgba, hexToRgbNorm,
  getAlignmentPositions, buildReservedMap, computeZigZagOrder,
  generateMaskGrid, evaluateMaskFormula, MASK_FORMULAS,
  STAGE_INFO, STAGE_COLORS, STAGE_DURATIONS
} from './codes/qr/qr-helpers.js';
